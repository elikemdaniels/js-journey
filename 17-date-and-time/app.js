// DATE & TIME

let currentDate = new Date();

currentDate = currentDate.setUTCYear(currentDate.getUTCFullYear());
// console.log(
//   `The current time is ${currentDate.getUTCHours()}:${currentDate.getUTCMinutes()}:${currentDate.getUTCSeconds()}`
// );

console.log(currentDate);
